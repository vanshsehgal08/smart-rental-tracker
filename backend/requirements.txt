
# fastapi>=0.104.0
# uvicorn[standard]>=0.24.0
# sqlalchemy>=2.0.0
# pydantic>=2.0.0
# python-dotenv>=1.0.0
# requests>=2.31.0
# pandas>=1.5.0
# numpy>=1.21.0
# scikit-learn>=1.0.0

# Smart Rental Tracker - Backend Dependencies
# Install with: pip install -r requirements.txt

# Core FastAPI Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Database ORM and SQLite
sqlalchemy>=2.0.0
alembic>=1.12.0

# Data Validation and Serialization
pydantic>=2.0.0

# Email and Notification System
python-dotenv>=1.0.0
schedule>=1.2.0
requests>=2.31.0

# ML Dependencies for Analytics and Forecasting (Windows-compatible versions)
pandas>=1.5.0
scikit-learn>=1.0.0
numpy>=1.21.0
matplotlib>=3.5.0
seaborn>=0.11.0
joblib>=1.1.0

# Data Processing and File Handling
openpyxl>=3.0.0
xlsxwriter>=3.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.20.0

# CORS Middleware (included with FastAPI)
# fastapi already includes starlette

# Built-in Python libraries (no pip install needed):
# - smtplib (email sending)
# - email (email formatting)
# - datetime (date/time handling)
# - typing (type hints)
# - os (operating system interface)
# - sys (system parameters)
# - time (time utilities)
# - logging (application logging)
# - argparse (command line parsing)
# - random (random number generation)
